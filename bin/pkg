#!/usr/bin/env bash

set -euo pipefail

source "config.sh"
source "utils.sh"

mkdir -p "$pkgdir"

for pkg in "$@"; do
    source "$workdir/$pkg/pkgbuild"
    log " => Building '$pkgname-$pkgver.pkg'"
    download
    prepare
    build
    package
done

log " => Finished succesfully"
